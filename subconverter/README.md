# Subconverter 配置说明

本目录包含用于 [Subconverter](https://github.com/tindy2013/subconverter) 的订阅转换配置文件。

## 📄 配置文件

### `basic.ini` - 基础版配置

**适用人群**: 新手用户
**功能特点**:
- ✅ 简单的地区节点分组（香港、美国、日本、新加坡、台湾）
- ✅ 基础分流（流媒体、Telegram、Apple、Microsoft）
- ✅ 广告拦截
- ✅ 自动选择最低延迟节点

**推荐场景**: 日常使用、追求稳定性、不需要复杂配置

### `advanced.ini` - 进阶版配置

**适用人群**: 高级用户、折腾爱好者
**功能特点**:
- ✅ 完整地区分组（包含韩国节点）
- ✅ **Relay 链式代理支持**（绕过部分限制）
- ✅ AI 服务专项分流（ChatGPT、Claude、Copilot、Gemini）
- ✅ 流媒体细分（Netflix、Disney+、YouTube、B站、巴哈姆特）
- ✅ 游戏平台（Steam、Epic、PlayStation）
- ✅ 开发工具（GitHub、Docker）
- ✅ 系统服务（Google、Microsoft、Apple）

**推荐场景**: 需要精细化分流、使用 AI 工具、观看流媒体、开发者

#### 节点候选顺序规范
- 标准顺序：`香港节点 → 美国节点 → 狮城节点 → 日本节点 → 台湾节点 → 韩国节点 → 节点选择 → DIRECT`
- 统一目的：提升使用时的操作一致性，避免不同分组下候选项位置变化
- 说明：此前部分分组为优化体验使用了特定优先级（如流媒体偏好某地区）；现已统一为标准顺序

---

## 🚀 使用方法

### 方法 1: 使用在线服务（最简单）

1. 访问任意 Subconverter 网页版：
   - https://sub.bonds.id/
   - https://sub.xeton.dev/
   - https://api.dler.io/sub

2. 填写以下信息：
   - **订阅链接**: 你的机场订阅地址
   - **客户端**: 选择 `Clash` 或 `ClashR`
   - **远程配置**: 填入下方链接（二选一）

**基础版**:
```
https://cdn.jsdelivr.net/gh/你的用户名/ACL4ALL@main/subconverter/basic.ini
```

**进阶版**:
```
https://cdn.jsdelivr.net/gh/你的用户名/ACL4ALL@main/subconverter/advanced.ini
```

3. 点击"生成订阅链接"
4. 复制生成的链接，导入到你的客户端

---

### 方法 2: 自建 Subconverter（推荐）

**优点**: 隐私安全、速度快、不受限制

#### Docker 一键部署

```bash
# 拉取镜像
docker pull tindy2013/subconverter:latest

# 启动容器
docker run -d --name subconverter \
  --restart=always \
  -p 25500:25500 \
  tindy2013/subconverter:latest
```

#### 使用方法

1. 访问 `http://你的IP:25500`
2. 在"远程配置"中填入本仓库配置文件的 **Raw URL**:
   ```
   https://raw.githubusercontent.com/你的用户名/ACL4ALL/main/subconverter/advanced.ini
   ```
3. 生成订阅链接

---

### 方法 3: 直接 URL 调用（进阶）

如果你熟悉 Subconverter 的 API，可以直接拼接 URL：

```
http://你的服务器IP:25500/sub?target=clash&url=你的订阅链接&config=配置文件URL
```

**示例**:
```
http://127.0.0.1:25500/sub?target=clash&url=https://example.com/sub&config=https://cdn.jsdelivr.net/gh/你的用户名/ACL4ALL@main/subconverter/advanced.ini
```

**参数说明**:
- `target`: 客户端类型（clash、surge、quan 等）
- `url`: 你的机场订阅链接（需 URL 编码）
- `config`: 远程配置文件地址（使用 CDN 加速）

---

## 🔧 自定义修改

### 添加新的节点分组

在 `.ini` 文件的策略组部分添加：

```ini
; 添加德国节点
custom_proxy_group=🇩🇪 德国节点`url-test`(德|DE|Germany|🇩🇪)`http://www.gstatic.com/generate_204`300,,50

; 加入主选择器
custom_proxy_group=🚀 节点选择`select`[]♻️ 自动选择`[]🇩🇪 德国节点`[]🇭🇰 香港节点`[]DIRECT
```

### 修改节点筛选规则

**正则表达式说明**:
- `(港|HK|Hong Kong|🇭🇰)` - 匹配包含这些关键词的节点
- `(?i)` - 不区分大小写
- `.*` - 匹配所有节点

**示例**:
```ini
; 只选择 IPLC 专线节点
custom_proxy_group=🚄 IPLC专线`url-test`(IPLC|专线)`http://www.gstatic.com/generate_204`300,,50

; 排除某些节点（使用负向前瞻）
custom_proxy_group=🚫 非美国节点`url-test`^(?!.*(美|US)).*`http://www.gstatic.com/generate_204`300,,50
```

### 添加自定义规则

在规则集部分添加：

```ini
; 引用本仓库的自定义规则
ruleset=🎯 全球直连,https://cdn.jsdelivr.net/gh/你的用户名/ACL4ALL@main/rulesets/custom/direct.list

; 或使用其他规则集
ruleset=🚀 节点选择,https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Clash/Twitter/Twitter.list
```

---

## 📝 策略组类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| **select** | 手动选择 | `select`[]节点1`[]节点2` |
| **url-test** | 自动选择最低延迟 | `url-test`(HK)`测试地址`间隔`容差`超时` |
| **fallback** | 故障转移 | `fallback`(US)`测试地址`间隔` |
| **load-balance** | 负载均衡 | `load-balance`(SG)`测试地址`间隔` |
| **relay** | 链式代理 | `relay`[]节点1`[]节点2` |

---

## ⚠️ 常见问题

### Q: 为什么生成的订阅没有分组？

**A**: 可能是以下原因：
1. ❌ 配置文件 URL 无法访问（检查网络）
2. ❌ 节点名称不匹配正则表达式（修改筛选规则）
3. ❌ Subconverter 版本过旧（更新到最新版）

### Q: 链式代理（Relay）是什么？

**A**: Relay 将两个节点串联使用，流量先经过第一个节点，再经过第二个节点。
- ✅ 适用场景：绕过部分限制、隐藏真实 IP
- ❌ 缺点：延迟增加、速度降低

### Q: 使用 jsDelivr CDN 后多久生效？

**A**:
- GitHub 文件更新后，CDN 缓存约 **5-10 分钟** 刷新
- 强制刷新：在 URL 后加 `?version=时间戳`
  ```
  https://cdn.jsdelivr.net/gh/User/ACL4ALL@main/subconverter/advanced.ini?version=20251208
  ```

### Q: 私有仓库可以用吗？

**A**:
- ❌ jsDelivr CDN **不支持**私有仓库
- ✅ 可以使用 **GitHub Token** 或 **自建 Subconverter**

---

## 🔗 相关资源

- [Subconverter 官方文档](https://github.com/tindy2013/subconverter/blob/master/README-cn.md)
- [ACL4SSR 规则集](https://github.com/ACL4SSR/ACL4SSR)
- [Clash 配置文档](https://clash.wiki/)

---

**有问题？欢迎提交 Issue！**
